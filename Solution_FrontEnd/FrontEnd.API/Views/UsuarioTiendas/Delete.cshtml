@model FrontEnd.API.Models.UsuarioTienda

@{
    ViewData["Title"] = "Delete";
}

<div class="container">

    <h1>Eliminar</h1>

    <h3>¿Estás seguro de que quieres eliminar esto?</h3>
    <div>
        <h4>UsuarioTienda</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdTienda)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdTienda)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdUsuario)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdUsuario)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="IdUsuarioTienda" />
            <input type="submit" value="Eliminar" class="btn btn-danger" /> |
            <a asp-action="Index">Volver a la lista de los usuarios de las tiendas</a>
        </form>
    </div>

</div>