@model FrontEnd.API.Models.UsuarioTienda

@{
    ViewData["Title"] = "Details";
}

<div class="container">

    <h1>Details</h1>

    <div>
        <h4>UsuarioTienda</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdTienda)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdTienda)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdUsuario)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdUsuario)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.IdUsuarioTienda">Editar</a> |
        <a asp-action="Index">Volver a la lista de usuarios de las tiendas</a>
    </div>

</div>